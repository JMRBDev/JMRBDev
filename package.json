{
  "name": "rss-to-readme",
  "version": "0.1.0",
  "private": true,
  "description": "Update a README section from an RSS feed",
  "type": "module",
  "packageManager": "npm@11.8.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "ncc build index.js -o dist",
    "test": "node --test",
    "check": "npm run test && npm run build"
  },
  "author": "JMRBDev",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "3.0.0",
    "rss-parser": "3.13.0"
  },
  "devDependencies": {
    "@vercel/ncc": "0.38.4"
  }
}
